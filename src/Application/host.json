{
  "version": "2.0",
  "$schema": "http://json.schemastore.org/proxies",
  "proxies": {
    "swagger": {
      "matchCondition": {
        "route": "/api/swagger/{*path}"
      },
      "backendUri": "http://localhost:{AzureWebJobs.HttpWorker.HttpWorkerPort}"
    }
  }
}